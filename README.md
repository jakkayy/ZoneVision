# ZoneVision - Location Intelligence for F&B in Bangkok

โปรเจกต์วิเคราะห์ทำเลศักยภาพ (Location Suitability Analysis) ในกรุงเทพฯ สำหรับประเภทร้านอาหารต่าง ๆ (ทั่วไป, คาเฟ่, ฟาสต์ฟู้ด, บาร์, บุฟเฟ่ต์ครอบครัว) โดยใช้ข้อมูลเชิงพื้นที่และ H3 Grid System ของ Uber

---

## 📁 โครงสร้างระบบ
* **`data-pipeline/data/raw/`**: ที่เก็บข้อมูลดิบ (เช่น ไฟล์ผังเมืองประเทศไทย `thailand-260703.osm.pbf` และสถิติสิ่งปลูกสร้าง `housing-infomation.csv`)
* **`data-pipeline/data/interim/`**: ข้อมูลที่สกัดออกมาจากการแปลงพิกัดเบื้องต้น (JSON)
* **`data-pipeline/data/processed/`**: ข้อมูลที่ทำความสะอาดพร้อมใช้งาน และผลลัพธ์คะแนนทำเลทอง (`bangkok_h3_opportunity_scores.csv`)
* **`data-pipeline/reports/figures/`**: แผนภูมิวิเคราะห์ค่าว่าง (Heatmaps) สำหรับประเมินความถูกต้องของชุดข้อมูล

---

## 🚀 ขั้นตอนและวิธีใช้งาน

### 1. เข้าใช้งานสภาพแวดล้อมเสมือน (Activate Virtual Environment)
เปิด Terminal ในโฟลเดอร์โปรเจกต์ (`ZoneVision`) แล้วเรียกใช้งาน `venv` ด้วยคำสั่ง:
```bash
source venv/bin/activate
```

### 2. อัปเดตและติดตั้งไลบรารีเพิ่มเติม
หากต้องการตรวจสอบว่าไลบรารีที่จำเป็นถูกติดตั้งครบถ้วนแล้ว ให้รันคำสั่ง:
```bash
pip install -r requirements.txt
```

### 3. วิธีรันระบบวิเคราะห์ทำเลทอง (Run Pipeline)
รันไฟล์ประสานงานหลักเพื่อทำความสะอาดและจัดเตรียมข้อมูล:
```bash
make pipeline
```
*(หรือใช้คำสั่งเต็ม: `python data-pipeline/run_pipeline.py`)*

### 4. วิธีประเมินศักยภาพทำเลรายจุด (CLI Suitability Evaluation)
คุณสามารถรันตัวประเมินทำเลเป้าหมายด้วยคำสั่งสั้นผ่าน **Makefile** (แนะนำ) หรือพิมพ์สั่งผ่าน Python ตรงๆ ดังนี้:

* **คำสั่งทางลัดด่วนผ่าน Makefile (แนะนำ):**
  ```bash
  make run centralworld general
  make run siam-square cafe
  make run nong-chok restaurant
  ```

* **คำสั่งแบบดั้งเดิมผ่าน Python (ระบุชื่อย่อ Presets):**
  ```bash
  python data-pipeline/scripts/analysis/evaluate_location.py siam-paragon cafe
  ```

* **คำสั่งแบบป้อนพิกัดตรงด้วยละติจูด,ลองจิจูด:**
  ```bash
  python data-pipeline/scripts/analysis/evaluate_location.py 13.7456,100.5342 restaurant
  ```

---

### 📍 รายการชื่อสถานที่ย่อและประเภทธุรกิจที่ระบบรองรับ (Reference List)

คุณสามารถป้อนชื่อสถานที่หรือประเภทธุรกิจเหล่านี้ในตัวสืบค้นได้ทันที:

#### 1. ชื่อสถานที่ย่อ (Location Presets)
| ชื่อย่อ (สำหรับพิมพ์สั่ง) | สถานที่จริง | พิกัดอ้างอิง (Lat, Lng) |
| :--- | :--- | :--- |
| `siam-paragon` | สยามพารากอน | `13.7458, 100.5350` |
| `siam-square` | สยามสแควร์ | `13.7456, 100.5342` |
| `centralworld` | เซ็นทรัลเวิลด์ | `13.7466, 100.5397` |
| `chula` | พื้นที่มหาวิทยาลัยจุฬาลงกรณ์ | `13.7384, 100.5307` |
| `asok` | แยกอโศกมนตรี | `13.7369, 100.5604` |
| `silom` | ถนนสีลม / ย่านศาลาแดง | `13.7286, 100.5340` |
| `ari` | ย่านซอยอารีย์ | `13.7796, 100.5447` |
| `thonglor` | ซอยทองหล่อ | `13.7339, 100.5828` |
| `chatuchak` | สวนจตุจักร / ตลาดนัด | `13.8034, 100.5532` |
| `victory-monument` | อนุสาวรีย์ชัยสมรภูมิ | `13.7649, 100.5383` |
| **`nong-chok`** | หนองจอกชานเมือง (ทำเลความหนาแน่นต่ำมาก) | `13.8584, 100.8653` |
| **`phutthamonthon-outer`** | พุทธมณฑลสาย 4 รอบนอก (ทำเลชานเมืองฝั่งตะวันตก) | `13.7594, 100.3226` |
| **`minburi-outer`** | มีนบุรีรอบนอก (ทำเลชานเมืองฝั่งตะวันออก) | `13.8138, 100.7225` |

#### 2. ประเภทธุรกิจอาหาร (F&B Types)
* **`restaurant`** (หรือ **`general`**): ร้านอาหารทั่วไป (ปิ้งย่างจานด่วน, ตามสั่ง)
* **`cafe`**: ร้านกาแฟ คาเฟ่ ขนมหวาน (เน้นกลุ่มคนทำงานและนักศึกษา)
* **`fast_food`**: ร้านอาหารบริการด่วน (เน้นย่านการค้าและห้างสรรพสินค้า)
* **`bar`**: ร้านเหล้า บาร์ สถานบันเทิงยามค่ำคืน (เน้นย่านที่อยู่อาศัยกลางคืน)
* **`family_buffet`**: ชาบู ปิ้งย่าง หมูกระทะ (เน้นกลุ่มลูกค้าแบบครอบครัว)

---

## 🧪 คู่มือการทดสอบระบบแบบเต็มรูปแบบ (End-to-End Testing Guide)

หากต้องการทดสอบตัวระบบ Data Pipeline ตั้งแต่ต้นน้ำถึงปลายน้ำ ให้ทดสอบดังนี้:

### ขั้นตอนที่ 1: ดึงพิกัดล่าสุดผ่านอินเทอร์เน็ต (Overpass API)
ดึงตำแหน่งคู่แข่งและตึกผู้ซื้อจากเซิร์ฟเวอร์หลัก:
```bash
make scrape
```
*(หรือคำสั่งเต็ม: รันสคริปต์ `scrape_competitors.py` และ `scrape_pois.py`)*

### ขั้นตอนที่ 2: รันคลีนและจัดโครงสร้างไพป์ไลน์ข้อมูล
ประมวลผลทำความสะอาดคัดแยกข้อมูลและวิเคราะห์อัตราการแข่งขัน:
```bash
make pipeline
```

### ขั้นตอนที่ 3: ตรวจสอบรายงานและแผนที่ปฏิสัมพันธ์
* เปิดเช็ครายงาน Heatmap ตรวจสอบค่าว่างได้ที่โฟลเดอร์: [data-pipeline/reports/figures/](data-pipeline/reports/figures/)
* เปิดดูแผนที่พล็อตคู่แข่ง Cluster บนบราวเซอร์ได้ที่ไฟล์: [data-pipeline/data/processed/bangkok_competitors_map.html](data-pipeline/data/processed/bangkok_competitors_map.html)

### ขั้นตอนที่ 4: รันวิเคราะห์จุดปักหมุด
ทดลองรันคำสั่งหาคำตอบศักยภาพพื้นที่ย่านเป้าหมาย:
```bash
make run asok cafe
make run nong-chok restaurant
```
*(หากใส่ชื่อย่อยที่ระบบไม่รู้จัก สคริปต์จะพ่นรายชื่อ Presets ทั้งหมดที่รอบรับในระบบออกมาแสดงทันที)*

